                        
                        ${townData.facts && townData.facts.length > 0 ? `
                            <h3 style="color: #333; margin-top: 30px;">📍 Local Facts</h3>
                            <div style="background: #f8f9fa; padding: 20px; border-radius: 10px;">
                                ${townData.facts.map(fact => `
                                    <div style="margin: 10px 0; padding: 10px; background: white; border-radius: 5px; border-left: 4px solid #667eea;">
                                        📍 ${fact}
                                    </div>
                                `).join('')}
                            </div>
                        ` : ''}
                        
                        ${townData.openaiInsights && townData.openaiInsights.length > 0 ? `
